# GoReleaser template for a tui-tools tool.
#
# Copy this to the tool's repository root as `.goreleaser.yaml` and replace
# every tui-cron with the tool name (`tui-firewall`). One repository, one
# binary, one tag series: `git tag v0.1.0 && git push origin v0.1.0`.
version: 2

project_name: tui-cron

before:
  hooks:
    - go mod download

builds:
  - id: tui-cron
    main: ./cmd/tui-cron
    binary: tui-cron
    env:
      # Static binaries: no libc dependency, one file to copy anywhere.
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{ .Version }}

archives:
  - id: tui-cron
    ids:
      - tui-cron
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    formats:
      - tar.gz
    files:
      - LICENSE
      - README.md
      - examples/config.toml

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  prerelease: auto
